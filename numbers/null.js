/* 

Abu reiskia reiksmes nebuvima/nezinojima

null - is pradziu as (programuotojas) nezinau, bet ketinu suzinot (null = 0)
undefined - paaiskeja kodo vykdymo metu
*/

const person = {
    name: 'Jonas',
};

console.log(person.name);
console.log(person.age);

let address = null;

console.log(address);
